const KEY = 'BOOK';

const SET_BOOK = `[${KEY}] SET BOOK`;
const CLEAR_BOOK = `[${KEY}] CLEAR BOOK`;

const SET_PAGE = `[${KEY}] SET PAGE`;

export default {
    KEY,
    SET_BOOK,
    CLEAR_BOOK,
    SET_PAGE
}